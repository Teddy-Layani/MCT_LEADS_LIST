<mvc:View controllerName="zcrmleadslist.controller.Main"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.f">
    <Page id="page" title="{i18n>title}">
        <!-- <l:Grid containerQuery="true" class="sapUiSmallMarginTop"> -->
            <HBox justifyContent="SpaceAround"
                items="{ path: '/ElementTypeCountSet',
                         filters: [{path : 'ElementType',operator : 'EQ',value1 : 'TILE'}]}">
                <GenericTile class="sapUiTinyMarginBegin sapUiTinyMarginTop tileSize" header="{UiText}"
                        frameType = "Auto" press="onPress" headerImage="sap-icon://kpi-managing-my-area">
                    <TileContent>
                        <NumericContent value="{LeadCount}" withMargin="true" />
                    </TileContent>
                </GenericTile>
            </HBox>
            <!-- <GenericTile class="sapUiTinyMarginBegin sapUiTinyMarginTop tileSize" header="{i18n>NEW_LEAD}"
                    frameType = "Auto" press="onPress" headerImage="sap-icon://kpi-managing-my-area">
                <TileContent>
                    <NumericContent value="1.3" valueColor="Error" withMargin="true" />
                </TileContent>
            </GenericTile>

            <GenericTile class="sapUiTinyMarginBegin sapUiTinyMarginTop tileSize" header="{i18n>HIGH_RANKING_LEAD}"
                    frameType = "Auto" press="onPress" headerImage="sap-icon://unfavorite">
                <TileContent>
                    <NumericContent scale="M" value="1.96" valueColor="Error" indicator="Up" withMargin="true" />
                </TileContent>
            </GenericTile>

            <GenericTile class="sapUiTinyMarginBegin sapUiTinyMarginTop tileSize" header="{i18n>LATE_TASKS}"
                    frameType = "Auto" press="onPress" headerImage="sap-icon://quality-issue">
                <TileContent>
                    <NumericContent scale="M" value="1.96" valueColor="Error" indicator="Up" withMargin="true" />
                </TileContent>
            </GenericTile>

            <GenericTile class="sapUiTinyMarginBegin sapUiTinyMarginTop tileSize" header="{i18n>MEETING_TODAY}"
                    frameType = "Auto" press="onPress" headerImage="sap-icon://date-time">
                <TileContent>
                    <NumericContent scale="M" value="1.96" valueColor="Error" indicator="Up" withMargin="true" />
                </TileContent>
            </GenericTile> -->
        <!-- </l:Grid> -->
        <SegmentedButton selectedKey="{mainView>/selectedSegmentButton}" width="97%" class="sapUiSmallMargin" 
            selectionChange="onSelectedButtonChange"
            items="{ path: '/ElementTypeCountSet',
                     parameters:{expand : 'TabConditionSet'},
                     filters: [{path : 'ElementType',operator : 'EQ',value1 : 'TAB'}]}">
            <items>
                <SegmentedButtonItem text="{UiText}({LeadCount})" key="{SortOrder}" />
                <!-- <SegmentedButtonItem text="{i18n>LEADS_PROCESSING}" key="PROCESS"/>
                <SegmentedButtonItem text="{i18n>LEADS_IRREGULAR}"  key="IRREGULAR"/>
                <SegmentedButtonItem text="{i18n>LEADS_OPEN}"       key="OPEN"/> -->
            </items>
        </SegmentedButton>

        <HBox id="filters-id" justifyContent="SpaceBetween" class="sapUiSmallMargin">
            <ComboBox placeholder="{i18n>STATUS}" class="selectRadius" selectedKey="{mainView>/selectedStatus}"
            	items="{
					path: '/ZALL_LEADS_STATUSSet',
					sorter: { path: 'Txt30' }
				}" selectionChange="onComboChange">
				<core:Item key="{Estat}" text="{Txt30}" />
			</ComboBox>
            <ComboBox placeholder="{i18n>RANK}" class="selectRadius" selectedKey="{mainView>/selectedPriority}"
                items="{
					path: '/ImportanceSet',
					sorter: { path: 'Description' }
				}" selectionChange="onComboChange">
				<core:Item key="{Importance_val}" text="{Description}" />
			</ComboBox>
            <ComboBox placeholder="{i18n>CAMPAIGN}" class="selectRadius" selectedKey="{mainView>/selectedCampaign}"
                items="{
					path: '/CampaignSet',
					sorter: { path: 'Description' }
				}" selectionChange="onComboChange" >
				<core:Item key="{Campaign_id}" text="{Description}" />
			</ComboBox>
            <ComboBox placeholder="{i18n>BRAND}" class="selectRadius" selectedKey="{mainView>/selectedBrand}"
                items="{
					path: '/BrandSet',
					sorter: { path: 'Description' }
				}" selectionChange="onComboChange" >
				<core:Item key="{Brand_code}" text="{Description}" />
			</ComboBox>
            <Button icon="sap-icon://reset"
                press="onFilterReset"/>      
            <DateTimePicker
                placeholder="{i18n>ENTER_DATE}"
                change="onDateChange"
                dateValue="{mainView>/date}"
                class="sapUiSmallMarginBottom"/>
        </HBox>

        <Table 
            id="mainLead-table"
            headerText="{i18n>TABLE_TITLE} ({mainView>/leadCount})"
            inset="false"
            updateFinished="mainLeadUpdateFinished"
            items="{
                path: '/MainLeadSet',
                sorter: {
                    path: 'StartDate'
                }
            }">
            <columns>
                <Column><Text text="{i18n>CUSTOMER}"        class="textBold"/></Column>
                <Column><Text text="{i18n>LEAD_NUMBER}"     class="textBold"/></Column>
                <Column><Text text="{i18n>CREATION_DATE}"   class="textBold"/></Column>
                <Column><Text text="{i18n>BRAND}"           class="textBold"/></Column>
                <Column><Text text="{i18n>OPEN_DATE}"       class="textBold"/></Column>
                <Column><Text text="{i18n>CAMPAIGN}"        class="textBold"/></Column>
                <!-- <Column><Text text="{i18n>SLA}"             class="textBold"/></Column> -->
                <Column><Text text="{i18n>PERSON_RESPONSIBLE}" class="textBold"/></Column>
                <Column><Text text="{i18n>RANK}"            class="textBold"/></Column>
                <Column><Text text="{i18n>STATUS}"          class="textBold"/></Column>
                <Column><Text text="{i18n>STATUS_REASON}"   class="textBold"/></Column>
                <Column><Text text="{i18n>FU_DATE}"         class="textBold"/></Column>
            </columns>
            <items>
                <ColumnListItem vAlign="Middle">
                    <cells>
                        <ObjectIdentifier
                            title="{ProspectName}"
                            text="{ProspectNumber}"/>
                        <Link
                            text="{ObjectId}"
                            target="_blank"
                            href="{Url}" />
                        <Text text="{path:'StartDate', 
                            type:'sap.ui.model.type.Date', 
                            formatOptions : { style:'medium', UTC: true}}"/>
                        <f:Avatar src="img/{Brand}.jpg" displaySize="M" displayShape="Square" imageFitType="Contain"></f:Avatar>
                        <Text text="{path:'StartDate', 
                            type:'sap.ui.model.type.Date', 
                            formatOptions : { style:'medium', UTC: true}}"/>
                        <ObjectIdentifier
                            title="{CampaignDescription}"
                            text="{CampaignId}"/>
                        <!-- <Text text="SLA"/> -->
                        <Text text="{PartnerRespName}"/>
                        <Text text="{PriorityTxt}"/>
                        <Text text="{StatusDesc}"/>
                        <Text text="{StatusReasonText}"/>
                        <Text text="{path:'StartDate', 
                            type:'sap.ui.model.type.Date', 
                            formatOptions : { pattern:'dd.MM.yyyy hh:MM:ss'}}"/>
                    </cells>
                </ColumnListItem>
            </items>
        </Table>
    </Page>
</mvc:View>
